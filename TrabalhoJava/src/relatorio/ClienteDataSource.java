package relatorio;

import java.util.Iterator;
import java.util.List;
import model.ClienteModel;
/*
import net.sf.jasperreports.engine.JRDataSource;
import net.sf.jasperreports.engine.JRException;
import net.sf.jasperreports.engine.JRField;
import model.UsuarioModel;*/

/*public class ClienteDataSource implements JRDataSource {

    private Iterator itrCliente;
    private Object valorAtual;

    private int i = 0;

    private boolean irParaProximoAluno = true;
*/
    /**
     * @param campos
     * @param itr
     */
   /* public ClienteDataSource(List lista) {
        super();
        this.itrCliente = lista.iterator();
    }

    public boolean next() throws JRException {
        valorAtual = itrCliente.hasNext() ? itrCliente.next() : null;
        irParaProximoAluno = (valorAtual != null);
        return irParaProximoAluno;
    }

    public Object getFieldValue(JRField campo) throws JRException {
        Object valor = null;
        ClienteModel cliente = (ClienteModel) valorAtual;*/

        /*if ("USU_ID".equals(campo.getName())) {
            valor = usuario.getUSU_ID();
        } else if ("USU_NOME".equals(campo.getName())) {
            valor = usuario.getUSU_NOME();
        } else if ("USU_LOGIN".equals(campo.getName())) {
            valor = usuario.getUSU_LOGIN();
        }*/

      /*  return valor;
    }
}*/